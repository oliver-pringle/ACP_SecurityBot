# build artifacts
**/bin/
**/obj/
**/node_modules/
**/dist/
# data + secrets
# Anchored to repo root so it ignores the runtime SQLite dir at ./data/ only.
# A bare `data/` matched the source folder SecurityBot.Api/Data/ case-
# insensitively on Windows/macOS, silently leaving Db.cs + the repositories
# untracked. Keep this anchored.
/data/
*.db
*.db-wal
*.db-shm
.env
**/.env
acp-v2/.env
ruvector.db
# scratch
_*.txt

# --- ruflo vector store / print dumps / IDE cache (cleanup guard 2026-06-07) ---
ruvector.db-*
.vs/
offerings.txt
*-for-paste.txt
print-offerings.out
print-resources.out
